{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for ALAX SSH Manager",
  "windows": ["main"],
  "permissions": ["core:default"]
}
